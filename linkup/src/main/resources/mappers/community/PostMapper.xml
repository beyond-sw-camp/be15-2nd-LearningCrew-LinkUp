<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.learningcrew.linkup.community.query.mapper.PostMapper">

    <!-- 게시글 전체 조회 (키워드, 사용자 조건 포함) -->
    <select id="selectAllPosts" resultType="com.learningcrew.linkup.community.query.dto.response.PostDTO">
        SELECT
            p.post_id,
            p.user_id,
            p.title,
            p.content,
            p.is_notice,
            p.created_at,
            p.updated_at
        FROM community_post p
        WHERE p.is_deleted = 'N'
        <if test="keyword != null and keyword != ''">
            AND (p.title LIKE CONCAT('%', #{keyword}, '%')
            OR p.content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="userId != null">
            AND p.user_id = #{userId}
        </if>
        ORDER BY p.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAllPosts" resultType="long">
        SELECT COUNT(*)
        FROM community_post p
        WHERE p.is_deleted = 'N'
        <if test="keyword != null and keyword != ''">
            AND (p.title LIKE CONCAT('%', #{keyword}, '%')
            OR p.content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="userId != null">
            AND p.user_id = #{userId}
        </if>
    </select>

    <!-- 게시글 상세 조회 (이미지 + 댓글은 이후 확장 가능) -->
    <resultMap id="PostDetailResultMap" type="com.learningcrew.linkup.community.query.dto.response.PostDetailResponse">
        <id property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="isNotice" column="is_notice"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectPostDetail" resultMap="PostDetailResultMap">
        SELECT
        post_id,
        user_id,
        title,
        content,
        is_notice,
        created_at,
        updated_at
        FROM community_post
        WHERE post_id = #{postId}
        AND is_deleted = 'N'
    </select>

    <select id="selectCommentsByPostId" resultType="CommentDTO">
        SELECT
            comment_id,
            user_id,
            content,
            created_at
        FROM post_comment
        WHERE post_id = #{postId}
        AND is_deleted = 'N'
    </select>

    <select id="selectPostImageUrlsByPostId" resultType="string">
        SELECT
            image_url
        FROM post_image
        WHERE post_id = #{postId}
    </select>


    <!-- 특정 회원 게시글 -->
    <select id="selectPostsByUser" resultType="com.learningcrew.linkup.community.query.dto.response.PostDTO">
        SELECT
            post_id,
            user_id,
            title,
            content,
            is_notice,
            created_at,
            updated_at
        FROM community_post
        WHERE is_deleted = 'N' AND user_id = #{userId}
        ORDER BY created_at DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countPostsByUser" resultType="long">
        SELECT COUNT(*) FROM community_post
        WHERE is_deleted = 'N' AND user_id = #{userId}
    </select>

</mapper>
